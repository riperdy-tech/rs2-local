# TEST PLAN: INSTITUTIONAL UNDERWRITING SYNTHESIS
**Version:** 1.0  
**Date:** 2026-09-19  
**Target Systems:** `RS2 Local` (Financial Modeling Desk, Analyst Tools, Capability Harness)

---

## 1. Objective & Governing Standards

This test plan validates the institutional separation of responsibilities:
1. **Lead Underwriter / PM (Qwen)**: Owns qualitative thesis, unit economics, scenario boundaries, re-entry tranches, and final capital allocation decisions.
2. **Financial Analyst Desk (Python)**: Owns exact arithmetic mid-stream (unbundled DCF, reverse DCF expectations gap, continuous multi-outcome Kelly sizing).
3. **Systemic Integrity**: Verifies zero hallucinated arithmetic, zero double-counted installed-base annuities, and strict fail-closed capital allocation when mathematical edge is non-positive.

---

## 2. Test Execution Matrix

### Phase 1: Deterministic Arithmetic Unit Tests (`test_financial_model_tool.py`)

| Test ID | Test Function | Target Behavior | Pass Criteria |
| :--- | :--- | :--- | :--- |
| **TC-1.1** | `test_unbundled_dcf_basic` | 5-year discrete DCF + terminal exit multiple - net debt | PV(DCF) + PV(TV) - Debt = Equity Value; positive per-share IV. |
| **TC-1.2** | `test_unbundled_dcf_with_annuity` | Separate capitalization of installed-base annuity stream | Annuity PV = Flow / Cap Rate; Enterprise Value = DCF PV + TV PV + Annuity PV (No double-counting). |
| **TC-1.3** | `test_reverse_dcf_expectations_gap` | Bisection search for 5y growth implied by $T_0$ market cap | Implied growth solved; expectations gap = Implied Growth - Demonstrated 5Y CAGR. |
| **TC-1.4** | `test_continuous_multi_outcome_kelly_negative_edge` | Empirical GEV distribution (35% Base @ $1100, 20% Bull @ $1450, 45% Bear @ $550 at $951 price) | Expected Return = -2.99%; **Full Kelly = 0.0%, Quarter-Kelly = 0.0%, Recommendation = NO_ALLOCATION**. |
| **TC-1.5** | `test_continuous_multi_outcome_kelly_positive_edge` | Asymmetric positive payoff distribution (50% Base @ $130, 30% Bull @ $170, 20% Bear @ $70 at $100 price) | Solves non-linear log-wealth optimization: Full Kelly > 0%, Quarter-Kelly = Full Kelly / 4. |
| **TC-1.6** | `test_calculate_payoff_skew` | Payoff ratio $(Bull - P) / (P - Bear)$ | Exact arithmetic matches formula (1.24x on GEV scenario). |
| **TC-1.7** | `test_execute_financial_model_dispatch` | Top-level dispatcher with full scenario payload | All calculation blocks present in output JSON; Kelly sizing clamped to 0.0% on negative edge. |
| **TC-1.8** | `test_unbundled_dcf_boundary_conditions` | Negative base cash flow ($CF \le 0$) or $WACC \le terminal\_g$ | Returns structured error dict; avoids crashes or mathematical exceptions. |
| **TC-1.9** | `test_calculate_payoff_skew_zero_downside` | Zero downside case ($Price == Bear\_IV$) | Avoids ZeroDivisionError; clamps to asymmetric ceiling (99.9x). |

### Phase 2: Tool Registration & Dispatch Integration (`test_analyst_tools_integration.py`)

| Test ID | Test Function | Target Behavior | Pass Criteria |
| :--- | :--- | :--- | :--- |
| **TC-2.1** | `test_financial_model_tool_registered_in_tools_list` | Ollama function calling schema in `analyst_tools.py` | `run_financial_model` in `TOOLS`; parameters `price` and `scenarios` defined. |
| **TC-2.2** | `test_dispatch_financial_model_tool` | Mid-stream function call dispatch and formatting | Invokes `execute_financial_model()`, formats markdown report for `<think>`, logs snapshot. |

### Phase 3: Prompt & Context Hygiene (`test_pack_enrichment.py`)

| Test ID | Test Function | Target Behavior | Pass Criteria |
| :--- | :--- | :--- | :--- |
| **TC-3.1** | `test_task_prompt_removes_manual_kelly_math_and_introduces_financial_desk` | Verification that manual mental Kelly derivation is deleted | Prompt contains NO instructions to manually derive $p, q, b, f^*$; contains PM role & `run_financial_model` instructions. |
| **TC-3.2** | `test_pack_contains_working_capital_float_notice` | Section 6 Customer Prepayment Float notice | Pack generated by `build_pack()` includes Working Capital Float & Prepayment Notice. |

---

## 3. Automated Test Execution Command

Run the complete test suite from the repository root:
```bash
python -m pytest "tools/audit_202608/tests" -v
```

**Expected Baseline:** 13 passed, 0 failed, 0 warnings.

---

## 4. End-to-End Live Ticker Verification Protocol

When validating on a real ticker run (e.g. `GEV` or `XOM`):

### Execution Command:
```bash
python tools/audit_202608/capability_test.py GEV --think high --ctx 81920
```

### Verification Checklist:
1. **Context Inspection (`_pack.md`)**:
   - Verify Section 1.5 contains live macro anchors ($R_f$, curve slope, credit spreads).
   - Verify Section 6 contains the Working Capital Float & Prepayment Notice.
   - Verify the prompt instructs Qwen as PM and references `run_financial_model`.
2. **Tool Invocation Trace (`_research_snapshot.json` / `_thinking.md`)**:
   - Confirm Qwen called `run_financial_model` with price and scenario assumptions.
   - Confirm Python returned the deterministic unbundled DCF and Kelly sizing table.
   - Confirm Qwen digested the spreadsheet numbers in its reasoning trace `<think>`.
3. **Deliverable Memorandum (`REPORT.md`)**:
   - Verify Section 12 JSON contract matches the exact numbers returned by Python.
   - Verify zero hallucinated Kelly formulas in prose.
   - Verify thesis invalidation triggers and re-entry tranches are clearly stated.
